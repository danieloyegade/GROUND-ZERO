<div class="persistent-ground-zero" aria-hidden="true">
  <p class="persistent-ground-zero__label">GROUND ZERO</p>
</div>

<style>
  .persistent-ground-zero {
    pointer-events: none;
  }

  .persistent-ground-zero__label {
    --rail-inline-end: var(--page-inline-offset);
    --rail-width: clamp(16.5rem, 25vw, 21.5rem);
    position: fixed;
    right: var(--rail-inline-end);
    bottom: clamp(1.75rem, 8vh, 4.75rem);
    z-index: 10;
    display: block;
    box-sizing: border-box;
    margin: 0;
    width: min(var(--rail-width), calc(100vw - (var(--rail-inline-end) * 2)));
    font-family: var(--font-sans);
    font-size: clamp(1.56rem, 3.1vw, 3.02rem);
    font-weight: 500;
    letter-spacing: 0.152em;
    line-height: 0.96;
    text-align: right;
    text-transform: uppercase;
    color: rgba(22, 20, 18, 0.48);
    font-kerning: none;
    font-variant-ligatures: none;
    white-space: nowrap;
    transition: opacity 180ms ease-out;
    pointer-events: auto;
    user-select: none;
  }

  .persistent-ground-zero__label:hover {
    opacity: 0.06;
  }

  @media (max-width: 40rem) {
    .persistent-ground-zero__label {
      --rail-width: min(18rem, calc(100vw - 2.5rem));
      bottom: clamp(1.35rem, 5vh, 2.4rem);
      font-size: clamp(1.44rem, 7.2vw, 1.9rem);
      letter-spacing: 0.142em;
    }
  }
</style>
