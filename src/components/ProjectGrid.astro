---
import ProjectCard from "./ProjectCard.astro";

interface ProjectEntry {
  title: string;
  image: string;
  descriptor: string;
  display: {
    orientation: "portrait" | "landscape" | "wide";
    cropFocus: "center" | "top" | "upper-third";
    size: "small" | "medium" | "large" | "hero";
  };
}

interface Props {
  projects: ProjectEntry[];
}

const { projects } = Astro.props;

const placementPattern: Array<Record<ProjectEntry["display"]["size"], string>> = [
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-1",
    medium: "col-span-12 md:col-span-6 xl:col-span-5 xl:col-start-1",
    large: "col-span-12 md:col-span-7 xl:col-span-6 xl:col-start-1",
    hero: "col-span-12 md:col-span-8 xl:col-span-7 xl:col-start-1",
  },
  {
    small: "col-span-12 md:col-span-4 xl:col-span-3 xl:col-start-8 md:mt-10 xl:mt-14",
    medium: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-8 md:mt-12 xl:mt-16",
    large: "col-span-12 md:col-span-6 xl:col-span-4 xl:col-start-8 md:mt-14 xl:mt-[4.25rem]",
    hero: "col-span-12 md:col-span-7 xl:col-span-5 xl:col-start-8 md:mt-16 xl:mt-[4.5rem]",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-3 md:mt-4 xl:mt-6",
    medium: "col-span-12 md:col-span-6 xl:col-span-5 xl:col-start-3 md:mt-4 xl:mt-6",
    large: "col-span-12 md:col-span-7 xl:col-span-6 xl:col-start-2 md:mt-5 xl:mt-8",
    hero: "col-span-12 md:col-span-8 xl:col-span-7 xl:col-start-2 md:mt-6 xl:mt-9",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-3 xl:col-start-8 xl:-mt-5",
    medium: "col-span-12 md:col-span-6 xl:col-span-4 xl:col-start-8 xl:-mt-7",
    large: "col-span-12 md:col-span-6 xl:col-span-4 xl:col-start-8 xl:-mt-7",
    hero: "col-span-12 md:col-span-7 xl:col-span-5 xl:col-start-8 xl:-mt-5",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-1 md:mt-10 xl:mt-14",
    medium: "col-span-12 md:col-span-6 xl:col-span-5 xl:col-start-1 md:mt-10 xl:mt-14",
    large: "col-span-12 md:col-span-7 xl:col-span-6 xl:col-start-1 md:mt-12 xl:mt-16",
    hero: "col-span-12 md:col-span-8 xl:col-span-7 xl:col-start-1 md:mt-14 xl:mt-18",
  },
  {
    small: "col-span-12 md:col-span-4 xl:col-span-3 xl:col-start-8 md:mt-[3.25rem] xl:mt-[4.25rem]",
    medium: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-8 md:mt-[3.75rem] xl:mt-[4.75rem]",
    large: "col-span-12 md:col-span-6 xl:col-span-4 xl:col-start-8 md:mt-16 xl:mt-20",
    hero: "col-span-12 md:col-span-7 xl:col-span-5 xl:col-start-8 md:mt-[4.5rem] xl:mt-[5.5rem]",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-2 md:mt-4 xl:mt-6",
    medium: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-2 md:mt-4 xl:mt-6",
    large: "col-span-12 md:col-span-6 xl:col-span-5 xl:col-start-2 md:mt-6 xl:mt-8",
    hero: "col-span-12 md:col-span-7 xl:col-span-6 xl:col-start-2 md:mt-8 xl:mt-10",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-7 md:mt-10 xl:mt-14",
    medium: "col-span-12 md:col-span-6 xl:col-span-4 xl:col-start-7 md:mt-12 xl:mt-16",
    large: "col-span-12 md:col-span-7 xl:col-span-5 xl:col-start-7 md:mt-12 xl:mt-16",
    hero: "col-span-12 md:col-span-8 xl:col-span-6 xl:col-start-7 md:mt-14 xl:mt-[4.5rem]",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-1 md:mt-10 xl:mt-14",
    medium: "col-span-12 md:col-span-6 xl:col-span-5 xl:col-start-1 md:mt-10 xl:mt-14",
    large: "col-span-12 md:col-span-7 xl:col-span-6 xl:col-start-1 md:mt-12 xl:mt-16",
    hero: "col-span-12 md:col-span-8 xl:col-span-7 xl:col-start-1 md:mt-12 xl:mt-16",
  },
  {
    small: "col-span-12 md:col-span-5 xl:col-span-4 xl:col-start-7 md:mt-[3.75rem] xl:mt-20",
    medium: "col-span-12 md:col-span-6 xl:col-span-4 xl:col-start-7 md:mt-[4.25rem] xl:mt-[5.5rem]",
    large: "col-span-12 md:col-span-6 xl:col-span-5 xl:col-start-7 md:mt-[4.5rem] xl:mt-[5.75rem]",
    hero: "col-span-12 md:col-span-7 xl:col-span-6 xl:col-start-7 md:mt-20 xl:mt-[6.25rem]",
  },
];

const frameMap: Record<ProjectEntry["display"]["orientation"], string> = {
  portrait: "aspect-[4/5] md:aspect-[4/5]",
  landscape: "aspect-[5/4] md:aspect-[5/4]",
  wide: "aspect-[16/10] md:aspect-[16/10]",
};
---

<section id="selected-work" class="px-6 pb-32 pt-6 sm:px-10 sm:pb-40 sm:pt-8 lg:px-14">
  <div class="mx-auto max-w-[96rem]">
    <div class="grid gap-10 md:grid-cols-12 md:gap-x-8 md:gap-y-[5.5rem] xl:gap-x-12 xl:gap-y-28">
      {
        projects.map((project, index) => {
          const placement = placementPattern[index % placementPattern.length];
          const layoutClass = placement[project.display.size];
          const frameClass = frameMap[project.display.orientation];

          return (
            <ProjectCard
              title={project.title}
              image={project.image}
              descriptor={project.descriptor}
              layoutClass={layoutClass}
              frameClass={frameClass}
              cropFocus={project.display.cropFocus}
            />
          );
        })
      }
    </div>
  </div>
</section>

<script is:inline>
  const revealTargets = Array.from(document.querySelectorAll("[data-project-reveal]"));

  if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
    revealTargets.forEach((element) => {
      element.setAttribute("data-reveal-state", "visible");
    });
  } else if ("IntersectionObserver" in window) {
    const revealObserver = new IntersectionObserver(
      (entries, observer) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;

          entry.target.setAttribute("data-reveal-state", "visible");
          observer.unobserve(entry.target);
        });
      },
      {
        threshold: 0.18,
        rootMargin: "0px 0px -8% 0px",
      }
    );

    revealTargets.forEach((element) => {
      if (element.getAttribute("data-reveal-state") === "visible") return;

      revealObserver.observe(element);
    });
  } else {
    revealTargets.forEach((element) => {
      element.setAttribute("data-reveal-state", "visible");
    });
  }
</script>
